1. 请你指导我使用supabase的cli，这样你以后可以直接修改和迁移数据库，保证本地业务逻辑和远程数据库是一致的
新增一个refunding阶段。并且给捐献记录新增一个语言选项，即用户下单时使用的语言：枚举为 en zh ua
2. 清理项目目录，删除不需要的文档，或放进/doc。整合supabase/下的文件，变为最终的几个新建数据库、新建视图和函数的sql文件
3. 新增一个组件，展示捐献流程：paied 支付成功，发送带有捐献编码的邮件 confrimed 一个工作日内ngo人工确认订单 delivering 捐赠执行中 completed 已完成，发送带照片/视频结果附件的邮件 发起refund后：refunding 在除了completed阶段均可以发起退款请求 refund：退款完成，7个工作日内完成
我希望做两种ui，横着的和竖着的，横着给桌面端首页用，竖着给移动端首页和所有端的捐赠详情页用
竖版：左侧一个竖直的进度条，有几个节点表示不同阶段，右侧为每个阶段的描述。放在首页项目画廊下，和每个捐赠项目页的项目详情下。首页这部分还要展示track donation按钮。
横版：水平的进度条，下方是描述

4. 新增一个组件，放在每个项目捐赠页底部，为一个列表，展示该项目所有的捐赠记录：
邮箱（模糊化）、金额、ID、时间、状态。如果是completed状态，请在最后放置一个查看结果按钮（先不写逻辑）
5. 优化查询页ui，展示该邮箱所有捐赠记录列表：
项目名（点击可跳转），捐赠id，金额，时间，状态。如果是completed，提供一个查看结果按钮（先不写逻辑），否则提供一个申请退款按钮（先不写逻辑）

6. 优化支付成功页面ui，显示此次生成的所有捐献id，提醒记录，并提示查看邮箱，提供track donation按钮。（先不编写发送邮件逻辑）

长期项目的total为NULL，并且画廊卡片只需要展示当前份数

8. 成功发送邮件逻辑、查看完成结果逻辑

确认refunding和refund状态的捐赠记录不参与计算


评估隐私泄露可能，url，数据库权限，只查询必要信息，在数据库层做模糊化

为画廊卡片设置背景（新建路径，如果没有路径/图片，则使用当前白色背景）

donate滚动

1. 移动端的mission section 调整样式：“我们的使命”紧挨着导航栏，最后一张卡片“受到袭击的平民”紧挨着下方的第二个section

2. 桌面端和移动端的项目捐赠页中的“追踪您的捐款”展示捐赠流程模块，都新增一个折叠功能，默认折叠

3.移动端的项目捐赠页下面的展示所有捐献记录模块：标题和捐赠id都是白色，和白色背景混淆了。重新设计移动端的样式，使得用户不需要横滑也可以查看完整表单

todo:

接入支付

验证支付成功页和邮件

圣诞节项目

验证乌克兰语翻译

将sponsor作为一个长期项目，编号0

付款收据

缩短响应速度





